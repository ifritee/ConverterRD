<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FileSystemActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:orientation="horizontal">
            <Button
                android:id="@+id/Back_btn"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:text="@string/back_button"
                android:onClick="BackDirectoryClick_v"/>

            <TextView
                android:id="@+id/full_path"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:text="@string/directory_name" />
            <Button
                android:id="@+id/Ext_btn"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:text="@string/ext_button"
                android:layout_gravity="right"
                android:onClick="ExtClick_v"/>
            <Button
                android:id="@+id/SD_btn"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:text="@string/sd_button"
                android:layout_gravity="right"
                android:onClick="SDClick_v"/>
        </LinearLayout>
        <ListView
            android:id="@+id/file_system"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </LinearLayout>

</android.support.constraint.ConstraintLayout>